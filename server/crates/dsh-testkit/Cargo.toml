[package]
name = "dsh-testkit"
version = "0.1.0"
license.workspace = true
edition = "2021"
description = "Defing config service - test fixtures (demo structure/value + project seeding)"

[dependencies]
dsh-core = { path = "../dsh-core" }
