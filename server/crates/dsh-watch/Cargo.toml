[package]
name = "dsh-watch"
version = "0.1.0"
edition = "2021"
description = "Defing config service - watch fan-out: broadcast hub + SSE (module 06)"

[dependencies]
dsh-core = { path = "../dsh-core" }
dsh-raft = { path = "../dsh-raft" }
axum = { workspace = true }
tokio = { workspace = true }
tokio-stream = { version = "0.1", features = ["sync"] }
futures = "0.3"
serde_json = { workspace = true }
